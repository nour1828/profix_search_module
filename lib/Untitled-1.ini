import 'package:flutter/material.dart';
// استدعاء الصفحات (تأكدي إن الأسماء دي هي نفس أسماء ملفاتك في الـ lib)
import 'role_selection_screen.dart'; 
import 'login_screen.dart';
import 'search_screen.dart';
import 'technican_requests_screen.dart'; // بالاسم اللي إنتِ مسمياه (بدون i)

void main() {
  runApp(const MyApp());
}

class MyApp extends StatelessWidget {
  const MyApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      title: 'ProFix',
      theme: ThemeData(
        primaryColor: const Color(0xFF3D5CFF),
        scaffoldBackgroundColor: Colors.white,
      ),
      // هنا حددنا إن أول صفحة تفتح هي صفحة اختيار (Customer / Technician)
      home: const RoleSelectionScreen(), 
    );
  }
}